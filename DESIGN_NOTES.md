# 后续玩法建议

以下是代码和关卡检查得到的建议。放门预览已实现统一绿色三格轮廓、方向箭头、虚线射线及无效红叉，复用实际放门判断。预览不再随门色切换，实际传送门仍保留蓝橙渐变。失败原因的具体文字提示仍可后续补充；其余建议按下文讨论。

## 优先改善反馈

1. **放门预览。** 在瞄准处显示三格候选、朝向和渐变顺序。失败时区分“灰墙”“三格不足”“门口被挡”“与另一门重叠”“本体所在门被锁定”“新投影会嵌墙”，玩家能知道是规则限制还是操作失误。穿门期间另一扇门仍可重放。
2. **降低精确对齐负担。** 58 像素身体通过 60 像素门只有 2 像素总余量；18 像素身体通过 20 像素窄道也是如此。当前碰撞严格按真实尺寸检查。可以考虑在接近门口时增加小幅、受碰撞限制的切向对齐辅助，而不是放宽碰撞后允许身体进墙。
3. **跳跃缓冲与离地宽限。** 缓存提前约 60～100ms 的跳跃按键，并允许离开平台后的短暂跳跃，改善边缘操作。需要作为明确的玩法参数测试，避免改变依赖精准落点的谜题。

## 核对规则和关卡难度

- **朝向是谜题的一部分。** 不建议默认自动扶正角色：关卡有窄道和身体朝向要求，自动扶正可能改变解法。若需要，可以提供独立辅助模式，并同时检查扶正后的完整碰撞箱是否有空间。
- **放门位置可以更友好。** 目前仍以命中格为中格检查三格墙面。可以在同一连续墙面内找最近的可放置三格区间，避免点在白墙边缘时失败，但不能自动跳到别的墙面。
- **明确定义正对墙面的方向。** 完全垂直射击时，头脚沿射线的投影长度为零，没有唯一切向。本次使用角色右侧方向作为稳定默认值。也可以设计一个显式翻转门方向的操作；这会影响现有通过射击角度控制方向的玩法，需要先确定设计意图。
- **按新物理重新试玩关卡。** 地图保持原样，但旧版的穿墙、取余纠正和速度截断不再存在。尤其应试玩 map10、map13、map15 的窄道、惯性和高低平台，决定是否需要微调地形或运动参数。
- **出口反馈。** 高亮可以使用的出口，并提示所需朝向；目前仍保留原来的 S 键与位置/姿态条件。

## 技术上还可改进

- 当前每次窗口定时器消息推进固定 20ms 物理时间。可以引入基于真实时间的固定步长累加器、限制追帧次数，并在失焦时暂停，让不同机器的速度更一致。
- 增加输入录制/回放和故障状态导出，把“偶尔卡住”保存成可重复测试，特别适合排查多个门、边角与窄道的组合。
- 如果将来加入移动墙体、旋转门或更多实体，应升级为连续碰撞查询或成熟的物理框架。当前小步 AABB 方案针对静态格子地图设计，不等于任意形状的通用物理系统。
