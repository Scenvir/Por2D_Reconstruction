# 天梯：不超过 19 次放门的路线复核

2026-09-21，按用户要求在 5 分钟内搜索。对象为源码地图 ID 16「天梯」，当前菜单最终关 Level 14，原编号 Level 12。未修改地图或物理代码，未覆盖用户录制。

用户原录制：`build/recordings/level-16-44221578.txt`。从出生点回放，2679 帧正常通关；共有 27 次射击输入，19 次实际改变门的位置或朝向（包括初次放门）。因此“19 门”与此统计口径一致，不等于原文件只包含 19 次点击。

| 记录 | 射击输入 / 有效门变化 | 通关帧数 | 模拟时间 | 性质 |
| --- | --- | --- | --- | --- |
| [route1.txt](route1.txt) | 19 / 19 | 1521 | 30.42 秒 | 与用户录制的后半段过门序列不同的路线变体 |
| [route2.txt](route2.txt) | 19 / 19 | 995 | 19.90 秒 | 用户原思路的精简版，不计作独立新解法 |

两份保存后的脚本均已使用 `build/Por2D.exe --headless --replay` 验证，结果为 `replay=cleared level=16`，正常触发最终关出口。可按 F6 导入；完整逻辑输入使用 `f` 指令，保留移动与射击同时发生的情况。

## 路线 1 的区别

原录制末段的主体过门顺序为：顶部 `(15,6)` 朝下 → 右下墙 `(37,18)` 朝上 → 中右墙 `(28,12)` 朝上 → 右上内墙 `(36,9)` 朝上 → 外侧墙 `(29,6)` 朝上。

路线 1 对应末段变为：顶部 `(15,6)` 朝下 → **右下墙 `(37,18)` 朝右 → 返回顶部 `(15,6)` 朝下 → 右下墙 `(37,18)` 朝上** → 中右墙 → 右上内墙 → 外侧墙。它多了一次横向出门与返回顶部的过程，却仍只需要 19 次放门输入。坐标为门起始格坐标，朝向指主身体朝向。

这是一条不同的通关路线变体，但仍沿用用户路线的整体门位体系，不能据此宣称发现了完全独立的新解题原理。路线 2 主要删去等待、试射与重复往返，核心思路沿用原录制。

## 搜索范围与限制

尝试了指令块删除、射击改为等待、成对删除射击、删门后调整附近移动时长或射击目标，以及在相近身体状态处拼接此前不同路线。以前四条自动搜索路线经过本轮局部优化，最低仍为 20、22、24、29 次有效门变化；它们未达到 19 门要求。

限时内找到上述 19 门路线变体与精简版，**未找到 18 门或更少的路线，也没有证明 19 门是最优值**。模拟时间不包含玩家思考、试错与真实瞄准时间。
